import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#FFC0CB", // rosa por defecto
    alignItems: "center",
    justifyContent: "center",
  },
  titulo: {
    fontSize: 34,
    fontWeight: "bold",
    color: "#fff",
    textAlign: "center",
  },
  creditos: {
    fontSize: 16,
    color: "#fff",
    textAlign: "center",
  },
});
